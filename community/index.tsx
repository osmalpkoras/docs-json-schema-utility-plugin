import { NavigationGroup } from '@/types/pages';

export const metadata = {
    kind: 'group',
    title: 'Community',
    description: 'Community resources for the JSON Object & Schema Utility plugin',
    order: 400,
} satisfies NavigationGroup;